apiVersion: v1
kind: Service
metadata: # kpt-merge: /onos-operator-app
  name: onos-operator-app
  labels:
    app: onos-operator-app
    chart: "app-operator-0.5.4"
    release: "onos-operator"
    heritage: "Helm"
  annotations:
    internal.kpt.dev/upstream-identifier: '|Service|default|onos-operator-app'
  namespace: kube-system
spec:
  selector:
    name: onos-operator-app
  ports:
  - name: webhook
    port: 443
    targetPort: 443
